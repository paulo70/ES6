(function() {
  const person = {
    name: 'karen campbel',
    sex: 'male',
    age: 40,
    city: 'new york',
    profission: 'skate'
  }

  const {name, sex, age} = person

  console.log(`${name} ${sex} ${age}`)
})()